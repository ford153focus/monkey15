document.querySelectorAll("article.cartCard").forEach(el => { console.log(el.querySelector('a.cartCardNameLink ').innerText + ` - (${el.querySelector('input[name="counter-input"]')?.value} x ${el.querySelector('div.cartCardFullPrice')?.innerText})`); })
